# Open Reviewr only when requested; keep it beside the active agent.
theme = "tokyo-night"
default_scope = "uncommitted"
navigator_position = "right"
toggle_placement = "split"
toggle_direction = "right"
auto_open = false
